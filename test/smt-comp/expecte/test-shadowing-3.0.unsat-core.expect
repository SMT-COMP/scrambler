(set-option :produce-unsat-cores true)
(set-logic ALL)
(declare-fun x () Bool)
(assert (! (let ((x x)) (let ((x x)) x)) :named smtcomp1))
(assert (! x :named smtcomp2))
(check-sat)
(get-unsat-core)
(exit)
